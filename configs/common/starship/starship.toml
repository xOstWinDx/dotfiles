"$schema" = "https://starship.rs/config-schema.json"

add_newline = true
palette = "onedark"

# Minimal, no filled segments: icons + palette fg only. SSH vs local differs via $hostname / $localip (ssh_only).
format = """
[╭─](bold gray4)\
$sudo\
$username\
$hostname\
$localip\
$directory\
$git_branch\
$git_state\
$git_status\
$python\
$cmd_duration\
$jobs\
$line_break\
[╰─](bold gray4)\
$character"""

[fill]
symbol = " "

[package]
disabled = true

[direnv]
disabled = true

[nix_shell]
disabled = true

# ── prompt character ───────────────────────────────────────────────────────
[character]
success_symbol = "[](bold green_bright)"
error_symbol = "[](bold red_bright)"

# ── identity & SSH (hostname + IP only on SSH) ─────────────────────────────
[username]
disabled = false
show_always = true
style_root = "bold red_bright"
style_user = "bold white0_reduce_blue"
format = "[ $user]($style) "

[hostname]
disabled = false
ssh_only = true
ssh_symbol = "󰌘 "
trim_at = ".local"
format = "[$ssh_symbol$hostname](bold red_bright) "

[localip]
disabled = false
ssh_only = true
format = "[󰩠 $localipv4](bold yellow_dim) "

# ── path ───────────────────────────────────────────────────────────────────
[directory]
read_only = " "
style = "bold cyan_bright dimmed"
truncation_length = 3
truncation_symbol = "…/"
home_symbol = "~ "
format = "[$read_only]($read_only_style)[󰉋 $path]($style) "

[directory.substitutions]
"Desktop" = "  "
"Documents" = "  "
"Downloads" = "  "
"GitHub" = " 󰊤 "
"Music" = " 󰎈 "
"Pictures" = "  "
"Videos" = "  "

# ── git (compact glyphs) ───────────────────────────────────────────────────
[git_branch]
symbol = "󰘬 "
format = "[$symbol$branch(:$remote_branch)](bold magenta_bright dimmed) "
truncation_length = 20
truncation_symbol = "…"

[git_state]
format = '[\($state( $progress_current/$progress_total)\)](bold red_bright) '
cherry_pick = "[󰏫 cherry](bold red_bright)"

[git_status]
style = "bold magenta_bright dimmed"
ahead = " 󰞙 "
behind = " 󰞀 "
conflicted = " 󰞇 "
deleted = " 󰆴 "
diverged = " 󰓒 "
modified = " 󰏫 "
renamed = " 󰏠 "
staged = " 󰄬 "
stashed = " 󰏌 "
untracked = " 󰋗 "

[git_commit]
disabled = true

# ── python / venv ──────────────────────────────────────────────────────────
[python]
symbol = " "
pyenv_version_name = true
detect_extensions = ["py", "ipynb"]
detect_files = ["pyproject.toml", "requirements.txt", "setup.py", "tox.ini", "Pipfile", ".python-version"]
detect_folders = [".venv", "venv"]
# Single-quoted: Starship needs `\(` for literal parens around `$virtualenv`; in TOML `"..."` that `\` breaks parsing.
format = '[$symbol${pyenv_prefix}(${version} )(\($virtualenv\) )](bold green_bright) '

# ── timing & privilege hints ───────────────────────────────────────────────
[cmd_duration]
min_time = 500
show_milliseconds = false
format = "[ $duration](bold yellow_bright) "

[sudo]
disabled = false
format = "[](bold red_bright) "

[status]
disabled = true

[jobs]
disabled = false
symbol = "󰜺 "
format = "[$symbol$number](bold yellow_base) "
threshold = 1

[line_break]
disabled = false

# ── unused modules (explicit off = faster) ────────────────────────────────
[memory_usage]
disabled = true

[time]
disabled = true

[container]
disabled = true

[os]
disabled = true

# ═══════════════════════════════════════════════════════════════════════════
# Palette: generated from configs/common/theme/palette.toml (scripts/render_theme.py)
### BEGIN GENERATED THEME (configs/common/theme/palette.toml)
[palettes.onedark]
black0 = "#1e2127"
black1 = "#282c34"
black2 = "#2c323c"
blue0 = "#78bfff"
blue1 = "#61afef"
blue2 = "#528bcc"
cyan_base = "#56b6c2"
cyan_bright = "#6cd9e8"
cyan_dim = "#4a9daa"
gray0 = "#1e2127"
gray1 = "#282c34"
gray2 = "#3e4451"
gray3 = "#4b5263"
gray4 = "#5c6370"
gray5 = "#7d8798"
green_base = "#98c379"
green_bright = "#a9dc76"
green_dim = "#82ad66"
magenta_base = "#c678dd"
magenta_bright = "#d896ff"
magenta_dim = "#a85ec9"
orange_base = "#d19a66"
orange_bright = "#e5a56f"
orange_dim = "#be8556"
red_base = "#e06c75"
red_bright = "#ff7b86"
red_dim = "#c9565f"
white0_normal = "#abb2bf"
white0_reduce_blue = "#a9b2c3"
white1 = "#cdd4e1"
white2 = "#dfe6f2"
white3 = "#ffffff"
yellow_base = "#e5c07b"
yellow_bright = "#ffd866"
yellow_dim = "#d4b068"
### END GENERATED THEME (configs/common/theme/palette.toml)
# ═══════════════════════════════════════════════════════════════════════════
